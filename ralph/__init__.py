"""Ralph: Copilot-CLI-driven autonomous loops (afk, grill).

The Python ports use the official ``github-copilot-sdk`` so every event
(reasoning, tool calls, tool output, compaction, usage) is rendered live —
matching the experience of running ``copilot`` interactively.
"""
